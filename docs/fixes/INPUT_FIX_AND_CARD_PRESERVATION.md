# 输入框修复与卡片样式保留

## 问题描述

### 问题1：输入框失去焦点后变深色
用户反馈：文本输入框在活动（focus）状态时是白色背景，但失去焦点后又恢复成深色背景。

**根本原因**：CSS规则没有覆盖所有输入框的伪类状态（`:hover`, `:focus`, `:active` 等），浏览器在不同状态下应用了系统的暗黑模式样式。

### 问题2：首页卡片失去大地色系配色
用户反馈：首页的卡片失去了原来温暖的大地色系渐变，变成了普通的白色。

**根本原因**：之前添加的强制亮色CSS规则过于激进，使用了通配符选择器覆盖了所有元素，包括自定义的卡片样式。

## 解决方案

### 1. 输入框所有状态的强制亮色

为输入框的**所有可能状态**添加CSS规则：

```css
/* 覆盖所有输入框的所有状态 */
.stTextInput > div > div > input,
.stTextInput > div > div > input:hover,
.stTextInput > div > div > input:focus,
.stTextInput > div > div > input:active,
.stTextInput > div > div > input:focus-visible,
.stTextArea > div > div > textarea,
.stTextArea > div > div > textarea:hover,
.stTextArea > div > div > textarea:focus,
.stTextArea > div > div > textarea:active,
/* ... 更多输入类型 ... */
textarea,
textarea:hover,
textarea:focus,
textarea:active,
select,
select:hover,
select:focus {
    background-color: #FFFFFF !important;
    border: 1px solid rgba(0, 0, 0, 0.15) !important;
    border-radius: 8px !important;
    color: #2B2B2B !important;
    color-scheme: light !important;
    -webkit-appearance: none !important;
    -moz-appearance: none !important;
    appearance: none !important;
}
```

关键点：
- ✅ 覆盖了 `:hover` 状态（鼠标悬停）
- ✅ 覆盖了 `:focus` 状态（输入焦点）
- ✅ 覆盖了 `:active` 状态（鼠标按下）
- ✅ 覆盖了 `:focus-visible` 状态（键盘焦点）
- ✅ 添加了 `color-scheme: light` 禁用浏览器暗黑模式
- ✅ 添加了 `appearance: none` 移除浏览器默认样式

### 2. 精确选择器 - 不影响卡片

修改了所有强制亮色的CSS规则，使用 `:not()` 伪类排除卡片：

```css
/* 不覆盖卡片内的元素 */
.main-title:not(.card-container .main-title),
.page-title:not(.card-container .page-title),
button:not(.card-container button),
[data-testid]:not(.card-container [data-testid]) {
    /* 强制亮色样式 */
}
```

这样确保：
- ✅ 卡片保留原有的渐变色背景
- ✅ 卡片内的文本颜色不被覆盖
- ✅ 卡片内的按钮样式不被影响
- ✅ 卡片的悬停效果正常工作

### 3. 媒体查询优化

修改了 `@media (prefers-color-scheme: dark)` 规则：

```css
@media (prefers-color-scheme: dark) {
    /* 强制输入框所有状态都是白色 */
    input, 
    input:hover,
    input:focus,
    input:active,
    textarea, 
    textarea:hover,
    textarea:focus,
    textarea:active {
        background-color: #FFFFFF !important;
        color: #2B2B2B !important;
        color-scheme: light !important;
    }
    
    /* 不覆盖卡片样式 */
    /* .card-container 的样式由原始CSS控制 */
}
```

### 4. data-theme 属性优化

修改了 `[data-theme="dark"]` 选择器：

```css
/* 只针对输入框，不影响卡片 */
[data-theme="dark"] input:not(.card-container input),
[data-theme="dark"] input:not(.card-container input):hover,
[data-theme="dark"] input:not(.card-container input):focus,
[data-theme="dark"] input:not(.card-container input):active {
    background-color: #FFFFFF !important;
    color: #2B2B2B !important;
    color-scheme: light !important;
}
```

## 修复后的效果

### ✅ 输入框
- 默认状态：白色背景 ✅
- 鼠标悬停：白色背景 ✅
- 获得焦点：白色背景 + 橙色边框 ✅
- 失去焦点：白色背景（不再变深色）✅
- 输入文字时：白色背景 ✅

### ✅ 首页卡片
- 卡片1：赤陶橙渐变 (`#E8957B → #D97A5E`) ✅
- 卡片2：温暖米渐变 (`#D4C5B0 → #C4B19D`) ✅
- 卡片3：浅棕渐变 (`#C8B8A8 → #B5A393`) ✅
- 卡片4：橄榄棕渐变 (`#A3957F → #8F8169`) ✅
- 卡片5：暖沙渐变 (`#D9B89A → #C9A282`) ✅
- 卡片6：奶油渐变 (`#E5D4C1 → #D4C2AD`) ✅
- 卡片7：灰褐渐变 (`#B8A89A → #A89688`) ✅
- 卡片8：肉桂渐变 (`#CEB5A0 → #BDA38C`) ✅

### ✅ 其他组件
- 背景色：温暖米黄 (`#F5F1E8`) ✅
- 侧边栏：浅米色 (`#FAFAF8`) ✅
- 面板：白色背景，正常显示 ✅
- 按钮：白色背景，不影响卡片内按钮 ✅

## 技术要点

### 1. 伪类状态覆盖的重要性

浏览器为表单元素定义了多个状态：
- **默认状态**：元素的初始样式
- **:hover**：鼠标悬停时
- **:focus**：获得键盘焦点时
- **:active**：鼠标按下时
- **:focus-visible**：键盘导航焦点时
- **:disabled**：禁用状态
- **:read-only**：只读状态

必须为每个状态都添加CSS规则，否则浏览器可能在某些状态下应用默认的暗黑模式样式。

### 2. :not() 伪类的使用

`:not()` 伪类允许我们排除特定的元素：

```css
/* 错误：会覆盖所有按钮 */
button {
    background-color: white !important;
}

/* 正确：不覆盖卡片内的按钮 */
button:not(.card-container button) {
    background-color: white !important;
}
```

### 3. color-scheme 属性

`color-scheme` 是控制浏览器原生暗黑模式的关键属性：

```css
input {
    color-scheme: light !important;  /* 告诉浏览器使用亮色主题 */
}
```

这会影响：
- 表单控件的默认样式
- 滚动条的颜色
- 系统原生UI元素

### 4. appearance 属性

`appearance: none` 移除浏览器的默认样式：

```css
input {
    -webkit-appearance: none !important;
    -moz-appearance: none !important;
    appearance: none !important;
}
```

这确保了跨浏览器的一致性。

## 测试清单

### 输入框测试
- [ ] 页面加载时：白色背景
- [ ] 鼠标悬停：保持白色背景
- [ ] 点击获得焦点：白色背景 + 橙色边框
- [ ] 输入文字：白色背景
- [ ] 点击其他地方失去焦点：保持白色背景（不变深色）
- [ ] 使用Tab键导航：白色背景

### 卡片测试
- [ ] 首页所有卡片显示正确的渐变色
- [ ] 卡片标题文字颜色正确（深色）
- [ ] 卡片描述文字颜色正确（中等深色）
- [ ] 卡片图标颜色正确
- [ ] 鼠标悬停：卡片有上移和阴影效果

### 系统模式测试
- [ ] 系统亮色模式：输入框白色，卡片有渐变色
- [ ] 系统暗黑模式：输入框仍为白色，卡片仍有渐变色
- [ ] 切换系统模式：应用保持一致外观

## 故障排查

如果输入框还是会变深色：

1. **硬刷新浏览器**
   ```
   Windows: Ctrl+F5
   Mac: Cmd+Shift+R
   ```

2. **清除浏览器缓存**
   - Chrome: 设置 → 隐私和安全 → 清除浏览数据
   - 选择"缓存的图片和文件"

3. **检查CSS是否加载**
   - F12 打开开发者工具
   - 选择输入框元素
   - 查看 Computed 标签
   - 确认 `background-color` 为 `rgb(255, 255, 255)`

4. **验证伪类样式**
   - 在开发者工具中点击 `:hov` 按钮
   - 勾选 `:focus` 和 `:hover`
   - 查看背景色是否改变

如果卡片没有渐变色：

1. **检查卡片HTML**
   ```html
   <div class="card-container card-gradient-1">
       <!-- 确保有 card-gradient-X 类名 -->
   </div>
   ```

2. **检查CSS规则**
   - 在开发者工具中查看卡片元素
   - 确认 `background` 属性有渐变值
   - 确认没有被其他规则覆盖

3. **重启Streamlit应用**
   ```bash
   Ctrl+C
   streamlit run homepage.py
   ```

## 相关文件

- `static/css/anthropic_theme.css` - 已大幅优化
- `homepage.py` - 使用卡片组件的主页
- `core/utils/theme_loader.py` - 主题加载器

## 更新日期

2025-10-21

## 总结

这次修复实现了：
1. ✅ **输入框在所有状态下都保持白色背景** - 解决了失去焦点变深色的问题
2. ✅ **卡片保留原有的大地色系渐变** - 使用精确选择器避免覆盖
3. ✅ **整体应用保持亮色主题** - 不受系统设置影响
4. ✅ **温暖的视觉体验** - Anthropic风格完整保留

现在的CSS规则既强制了亮色主题，又保留了自定义的设计元素！🎨✨


